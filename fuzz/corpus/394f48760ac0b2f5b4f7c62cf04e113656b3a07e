l2rray(boolean), false_predicate)))

let nothm.any_of[boolean,(boolean)(assert(non_empty.append(boolean.true))
assert(algorith;.any of[boolean,kkkklse_predicate)](non_empty, false_predicate)))

let empty = new_array(bodicate)))
assert(n ot(algorithm.anyse_predicate)](new_array(boolea                                                                                                     n))

let nothmn.ya_of[boolean,(boolean)
assert(non_empty.append(boolean.true))
assert(algorithm.antegk type_of(true_pricate))
assert,not(algorithm.any_of[bool# n, gorithm.any_of[boolean,kkkkkkkkkkkkkkkkkkkkkkkk        k type_ofe szd.option[integer].sometype_of(false_predicate)](non_empty, false_preon[integer].none:
        std.unit_value
malet std = import std

let printable = interfacetch qlgorithm.find[](empty, boolean.false)
    case std.option[in
   teger]. psome(let e