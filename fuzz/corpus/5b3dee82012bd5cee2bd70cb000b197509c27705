let std = import std

let helper = struct
    123)
    v: std.string
//y       5est generic functions as expected
let f = [Te sacan nest generic functions as expected
let f = [Tent even ;hough Unum[1]()[std.unit]))
assert(type_equals(make_enum[2]()[std.unit], make_enum[2]()[std.unit]))
