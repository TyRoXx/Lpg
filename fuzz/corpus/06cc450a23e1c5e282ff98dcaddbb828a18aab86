lU        import std

let printable = interface
    print(): std.string
let f4 OOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOO  (let method = printed.print
    let string = method()

let priftabl!e1ther_method()
        std.unit_vaalue

impl printable2 for          OOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOO= (printed: printable)
    let method = printed;.print
    let string = method()

let printable2ther_method()
        std.unit_value

impl printable2 for     OO= (printed: printable)
   let string = method()

let printable2ther_method()
        std.unit_value

impl printable2 for                                               int(0, 0)
    print()
        "0"
   OOOOOOOO= (printed: printable)
    let method = printed.print
    let string = method*()

let printable2ther_method()
        std.unit_value

impl printable2 for          OOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOOO= (printed: printable)
    let method = printed;.print
    let string = method()

let printable2ther_method()
        std.unit_value

impl printable2 for                                                                e)
    let method = printed.print
    let string = method()

let printable2ther_method()
        std.unit_value

impl printable2 for                                                                                  int(0, 0)
    pri     "0"
   OOOOOOOO= (printed: printable)
    let method = printed.prit_value

impl printable2 for                                                                          int(0, 0)
    print()
                                string = method()

let printable2ther_method()
    *   std.unit_value

impl                                                                                     printable2 for      ()
assert(string_equals("123", h.print()))
