  xw_array(std.arraay[string]])
let b = new_array(std.array[stringing].sueecolnd(runti
me_string("abc"))
asspile_ tristate.a:: e}))

let nested = struct
    a: struct
        b: boolean pa, s
let two_parameters = enum[First, Secos_pa _enum[2]()[std.unructure{"abc", boolean.true}). two_b(let s):
[boor= (h caseost tristate.a:: e}))

let nested = struct
    a: struct
        b: boolean pa, s)
)

let two_parameters = enum[First, Secos_parameter[access_parametecoe 8 _enum.b

