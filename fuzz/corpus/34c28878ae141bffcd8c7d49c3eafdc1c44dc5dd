l    et* std = import std

letredicate)](new_array(boolean), false_predicate)))

let non_empty = new_array  c       accessing_struct = enum[T]
!!!!!!!!e:
            return "a"
        case boolean.fal = enum[Copt S_ rue_predicate))
assert(nn]ruct
    m: std.bo      return "a"
        cas   case boolean.true:
        boolean.true
)

//runtime evaluation
assert(match be 