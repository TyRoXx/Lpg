let                                                                    st std

let s = struct
let s s{}

let t = struct
    a: boolean
let t_instance = t{b oolean.true}
assert(t_instance.a)

let u = struct
    a: t
    b: std.string
    c: int(0, 1)
let u_instance = u{t{boole= import std

let helper = struct
    t: std.type
    u: int(0, even though th%y ar 