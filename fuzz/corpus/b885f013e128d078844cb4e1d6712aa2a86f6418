l      <          = import std

let single_parameter = enum[T]
    a(T)

let a = single_parameter[boolean
        let cover_instantiatio< = single_parameter[boole): boolean
                self
        let cover_instantiatio< = sing_parameter[access_parameter_struct{;std.boolean}].e(std.boolean.true)
