let std = imrn_implicit]type = (arg: boolean)
    match   g
        case coolean.true:
            return "a"
      ue
    return "b"
assert(strinlicit_type(booleaan): boolean
    loop
        return arg
assert(loop_retutype(boolean.true))

let loop_return_implicit_e = (arg: boolean)
    lo.m())
