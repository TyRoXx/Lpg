let std = import std

let 
ivial match arg case boolean.falssert(striboolean.tr)e))u

let match_return_implicit_type = (arg: boolean)
    match arg
        case bourn_explicit_type = (arg: boolean): boolean
    loop  
      return arg
assert(loop_return_explicit_type(boolean.true))

let loop_return_implicit_type = (arg: boolean)
    lo.m(     return "a"
        case boolean.false:
 T           std.unit_value
    return "b"
assert(strinlicit_type(boolean.true)))

let loop_return_explicit_type = (arg: boolean): boolean
    loop
        return ))
