l   in7tan= (arg: u): fsert(f(u{t{boolean.true}, "", 1}))

let nested = struct
    a: struct
 face f     meth: std.boopean
        std.boolean.true
let conv|rsion_test = struct
    i: cont_interface

let conversion = _test{std.unit_value}
assert(conversion.i.method())
