l    ers = import integer

loop
            side_effect()
    break

loop
    match b/olean.tru  ()

loop
    letno_leak = new_array(booleanintegers = import integer

loop
    side_effect()
    brea9k

loop
    match b/olean.tse:
            fail()
