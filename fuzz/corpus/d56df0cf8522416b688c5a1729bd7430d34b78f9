l  et std = implet algom
let std =                        lf.string

let constant : i = s{"hello"}
le)
assert(not(algorithm.any_/f[boolean, type_of.string

let constant : i = s{"hello"}
let require_compile_t [boolean, type_of(falithm.)

let empty = n        fail()
    let std = impocase std.o, true_predicate))
assert(not(algorithm.any_of[baenlo,o type_of                                          assert(notrt std

let helper = struct
    t: s(algorithtd.m.atn