l   p             import std

lert(not(host.equ;als(m.equals(host.export_integer(0), host.export_string( p"0"))))

//
(4global: host_value, host: es.host)
;    test_string(host)
    test_rhost.export_string("0"))))

//
(global: host_value, host: es.host)
    tes               )

//
(global(: host_value, host: es.host)
     ener!!!!(!!!!!!
