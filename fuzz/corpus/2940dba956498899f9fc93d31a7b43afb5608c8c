let std = importe  
let c_ pr)itn
(        "uni;"
    other_method()
 `      std.unit_value

impl printable2 for int(0, 0)
    prilet std = import std
lehift_rigunit let print = a.print
    let s = print()
    s

impl printable2 for std.string
    print(): std.string
    nit
(        "unit"
    other_method()
        std.unit_value

impl printable1 for int(0, 0)
    prilet std = importd
let integers = import inct>= enum[T]
)