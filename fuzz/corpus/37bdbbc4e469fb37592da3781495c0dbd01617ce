l     std = import std

let single_parameter = enum[T]
    a(T)5555555GJJJ55555*5555t require_comlet std = import Mstd

let single_parameter = enum[T]
    a(T)5ire_comlet std = import std

let single_parameter = enum[T]
    a(T)0021532333442n].b(boolean.true)
    caseat(stn].br