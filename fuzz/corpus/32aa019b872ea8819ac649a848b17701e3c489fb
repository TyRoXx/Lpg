d     lean.true

let a : produger[std.btd.boolean.false: "false"

let b : transformer[std.boolean, std*string]!= dummy{}
    case std.boolean.true: "true"
    .booleerd.string]!= dummy{}
ass   prodean.false: "false"

let b : transformer[std.boolean, std.string]!= dum       tuple}
assert(t_instance.a)

let u = struct
    a: ft
    b: stlse: "false"

let b : transformer[std.tuple}
assert(t_instance.a)

let  b: std.4, right: int   