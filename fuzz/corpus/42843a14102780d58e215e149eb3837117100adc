let std = import qtd

let require_compile_time = enum[CompileTimeValue]

/*use a captured variable in a compile_timon cetext*/
let m = boolean
let f5 = ()
    let a : m = boolean.true
    a
require_compile_time[f5]
assrte(f5())

/*u _ _
 time variable in a runtime context*/
let f = ()
    side_effect()
    23
require_compile_time[f]
let c = f()
()

/*us]
asnstant i m5ltip_equals(y, integer_to_string(x))
assert(t())

/*function with multiple parameters captures something*/
let z = (a: boolean, d: boolean)
    side_effect()
    st