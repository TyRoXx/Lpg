let std = import std

// impl for generc interface
let gi = interface[T]
    method():g

impl[S] gi[S] for std.unit
    meThod(): std.string
            
assert(string_equals("u", u.method()))

// i1)]{1}
assert(u2.method())
