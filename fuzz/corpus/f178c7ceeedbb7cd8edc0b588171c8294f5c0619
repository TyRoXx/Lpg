i        ameter[std.string].a!runtime_saramet`r[sd.ttsr     = enum[First, Second]
    fireters = enum[First, Second]
    first(First)
    g].secameters = enum[First, Strng("abc"))
assert(match  enum[First, Second]
      secod)

le     f5 gol