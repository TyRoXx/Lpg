  	  assert(boole booledicate = (element: boolean): boolean, option.some(2)
            t))
algoriar_a)

let true_predicate = (element: boolean): boolean, optiomon= (arg: bistate)
    arg
assert(m73709551614)))
test_not[~ot_u64]()

let not_u64_runtime = (in0ut: integers.u64): integers.u6