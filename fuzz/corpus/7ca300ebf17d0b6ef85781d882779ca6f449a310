lL        = import std

let re    quire_compile_time = enum[CompileTimeValue]

/*use aime*/
let f6 = (a: boolean, b: boolean)
    let indirection = () a  /*use a captured variable in a compile_timlean.true)(ile time*/
let f6 = (a: boolean, b: boolean)
    let indirection = () a
    (c: std.unit, d: std.unit)
        side_effect()
 (a: boolean, b: boolean)
    let indirection = () a
    (c: std.unit, d: std.unit) an)
    side_effec               let indireuals(c, c)
assert(capture_twice())
