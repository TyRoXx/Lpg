let std = import std

let i
assert(n",             a((let state):
            side_effect( 
            string_equals("abcay_type = std.array[string]
let a : array_type = new_)array(string)
asserls(0, a.abc", mattring
 ;  y: boolean
let ")))
assert(not(a.stiore(1, "")))
