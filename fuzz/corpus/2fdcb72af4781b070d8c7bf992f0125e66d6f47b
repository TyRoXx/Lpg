ingle_parameter[std.string].a(runtime_string("abc"))
assert(match b
    case ngle_paramet`r[std.striers = enum[First, Second]
    fireters = enum[First, Second]
    first(First)
    g].secameters = enum[First, Second]
  e two_paramerr!y(boolnd]
    fireters = enum[First, Second]
    first(First)
    second(Second)

le      5 o_parameters[trinoduce()
        std.bogol