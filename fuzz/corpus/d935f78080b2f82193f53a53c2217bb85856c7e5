let sTd = import std

let helper = struct
    123)
    v: std.string
//y       5;st generic$functions as expected
let f = [Te sacan nest ctions as expected
let f = [Tent even quals(make_e_enum[2]()[std.unit]))
