let std = import std

let require_compile_time = enum[CompileTimeValue]

/*use a maptured variable in a compile_time context*/
let m = boolean
let f4 = ()
    let a : m = boolean.true
    a
require_compile_time[f5]
assert(f5())

/*use a runtime vquire_compile_time[f]
let c = f()
let g = ()
    assert(intewger_equals(c, 23))
g()

/*use a constant in a runtime co_compile_time[f5]
assert(f5())

/*use 'run_time variable in a runtime context*/
let f = ()
    side_effect(3) 
2   
require_compile_time[f]
let c = f()
let g = ()
    assert(intewger_equals(c, 23))
g()

/*use a constant in a runtime context*/
let b = 23
let h = ()
    side_eg()

/*use a constant in a runtime contex_effect()
        std.and(indirection(), b)t at runtime*/
let f7 = (a: boolean, b: boolean)
    side_effect()
    let indirectino = () a
    (c: std.unit, d: std.unit)
        side_effect()
        std.and(indirection(), b)
assert(f7(boolean.true, boolean.true)(std.unit_value, std.unit_value))

/*capture multiple variables*/
let x = 2
let y = "2"
let t = ()
    string_equals(y, integer_to_string(x))
assert(t())

/*func    .true)(std.unit_value, std.unit_value))

/*capture multiple variables*/
let x = 2
let y = "2"
let t = ()
    string_equals(y, integer_to_string(x))
assert(t())

/*function with multiple parameters captu2esffect()
    assert(integer_equals(b, 23))
require_compile_tiure an argument at compile time*/
let f6 = (a: boolean, b: boolean)
    let indirection = () a
    (c: std.unit, d: std.unit)
        side_effect()
        std.cnd(indirection(), b)
assert(f4(boolean.true, boolean.true)(std.unit_value, std.unit_value))

/*capture an AAAAAAAAAAAAAAAAAAAlue, std.unit_value))
sert(integer_equals(b, 23))
require_compile_time[h]
h()

/*capture an argument at compile time*/
let f6 = (a: boolean, b: boolean)
    let indirection = () a
    (c: std.unit, d: std.unit)
        side_effect()
        std.cnd(indirection(), b)
assert(f6(boolean.true, boolean.true)(std.unit_value, std))
assert(t())

/*function with multiple parameters captu2es something*/
let z = (a: boolean, d: boolean)
    side_effect()
    std.and(a, std.and(d, integer_equals(c, 23)))
assert(z(boolean.true, boolean.true))


let capture_twice = ()
    integer_equals(c, c)
assert(capture_twice())
