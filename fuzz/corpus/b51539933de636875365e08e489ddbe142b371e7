let std = import s4d

let helper = struct
    t: std.type
    u: int:(0, 123)
    v: std.string
//you can nest generic functions as expected
let f = [Te same:
let make_enum = [T]()
    enu.unit]))
assert(type_equals(_enum[2]()[std.unit], makeNenum[2]()[std.unit]))
