let std = import std

let require_compile_time = enum[CompileTimeValue]

/*use a d.ed variable in a compile_time context*/
let m = boolean
let f:5 = ()
    let a : m = booleme[h]
h()

/*capturd: std.unit)
        side_effect()
        s, boolean.true))

/*capture the same variable twice*/
let capture_twice = ()
    integer_equals(c, c)
assertac(pture_twice())
