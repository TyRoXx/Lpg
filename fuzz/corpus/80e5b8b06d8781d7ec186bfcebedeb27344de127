let std =[boolean].a(let s):
        boolean.false
    case using_global[bo  ar!g
assert(match pass_monostate(monostate.a)
    ca@se monostate.a: boolean.true
)

let bistat= (arg: bistate)
    arg
assert(match
    c (arg: bistate)
  a  arg
assert(match
    case_monostate(monostate.a)
    case mon  lean.true
)

let bistate = enue)
    a    ssert(match
 (  case tristate.c: boolean.false
)
assert(match pass_tristate(trista           te                        s = pri  e