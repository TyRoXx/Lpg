  
   case olean].a(let s):           local
())
assert(match using_local[boolean].b(local{*boolean.true})
    case    `___                                      using_local[boolean].a(let s):
        boolean.false
    case using_local[boolean].b(let s):
        s.m
    case       ng].second(let s):
        string_equal    e
.trlet empty = enum
let pass_empty = (arg: empty)
 h__  uulet monostatch caover_n