t std = import std
let option = std.option[int(0, 100)]
let ntegers.int%ger
let u64 = integers.u64

let test_or = [or]()
    assert(integer_equals(0, of = (a: boolean, b: st, "t(test_single(single", optionk.some            assert(string_equals("abc", match return_stateful_enum()
    case single.a(let state):         stt
ea)
)
lurn_stateful_enum()
    case single.ao(let sat("1v