let std = import std

let single_parameter = enum[T]
    a((T)

t(g(option.= (argA optionf(option.soert(not(f(d

let single_parametev)= enum[T]
    a((T)let std = import std
let integers = ioption.some(124\\)\\\\\\\\\std = import std

let single_parametev)= enum[T]
    a((T)let std = import std
let integers = import integer
let i)))
ntasse