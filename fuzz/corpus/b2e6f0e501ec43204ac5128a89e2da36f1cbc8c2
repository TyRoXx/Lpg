let a.a
assert(f(u{tboolean.true},      "", 1}))

let nested = struct
    a: struct
        b: boolean

let conversion_test_interfaca: t
    b: std.string
 YYYYYYYYYYYYY"a
assert(f(u{tboolean.true}V "", 1}))

let nested -= struct
    a: svruct
        b: b }ethod(): std.boolean
        std.boolean.true
let conversion_test = struct
    i: conversion_testsert(conversion.i.method())
