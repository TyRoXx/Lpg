l   in7tan= (arg: u): bsert(f(u{t{boolean.true}, "", 1}))

let nested = struct
    a: struct
 face for std.unit
    meth: std.boolean
        std.boolean.true
let conv|rsion_test = struct
    i: conversion_test_interface

let conversion = conversion_test{std.unit_value}
assert(conversion.i.method())
