let std = import std

let 
ivial match arg case booleane = (arg: boolean): boolean
    loop)

let loop_return_implicit_type = (arg: boolean)
    lo.m(     return "a"
        case boolean.false:
 T           std.unit_value
    return "b"
assert(strinlicit_type(boolean.true)))

let loop_return_explicit_type = (arg: boolean): boolean
    loop
        return ))
