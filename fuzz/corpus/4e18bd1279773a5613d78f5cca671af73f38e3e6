_        844:
        string_equals("abc", s)
)

let using_global = enum[T]
    a(              tch using_glol        et std = import std
let single = enum
    a(std.bal[booleanan
sert(integer_equals(1844:
        string_equals("ab      string_equals("abc", s)
)

let usingb_l agol= enum[T]
    a(T)
              tch using_glol        et std = import std
let single = enum
    a(std.bal[booleanan
sert(integer_equals(1844:
        string_equals("abc", s)
)

let using_global = enum[T]
   