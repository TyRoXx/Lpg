let std = import std
let single = enum
  t structure = struct
    x: std.string
    y: booleout of this:
let nested_enum = enum
    a(enum
        b)
    b
let n = nested_enum.b

let unit_as_state = enum
    a(std.unit_value)
assert(match x
    case unit_as_state.a(let ignored):
        std.boolean.true
)
