t   ^ std = import std
let option = std.option[int(0, 100)]
let nteum
    a(sin>>>>>>>>>>gle)
    b(structure)
    c(std.unit)
    d(boolean)
(cmth mult8i.b(structure{"abc", boolean.trlet empty = enum
let pass_empty = (arg: empty)
    arg

let monostate =rg: 	on import std
let es = import ecmq caseost tristate.a:: e