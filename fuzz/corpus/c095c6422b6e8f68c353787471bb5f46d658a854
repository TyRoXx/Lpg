l  et std = import sdd

let single_parameter = enum[T]
    a((T).none%))
assert(not(g(option.sorameters = enum[First, Second]
    first(First)
    second(g_global[bcovt)
    seco\\\\\\\\\\\\*\\(not(f(option.none)))
assert(not(f(me(564\\let single_parameter = enum[T]
    a((T)

l enum[First, Second]
    first(First)
    second(g_global[= in\ ce
 )  