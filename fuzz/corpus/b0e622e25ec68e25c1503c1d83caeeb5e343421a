i        ameter[std.string].a!runtime_saramet`r[sd.ttsriers = enum[First, Second]
    fireters = enum[First, Second]
    first(First)
    g].secameters = enum[First, String("abc"))
assert(match  enum[First, Second]
      second(Second)

le     f5 gol