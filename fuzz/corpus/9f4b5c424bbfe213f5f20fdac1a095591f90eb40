l    et std = import std

let trivial match arg
       loop
       return_implict_tyipe(boole  c       accessi!!!!!!!!!!!!!!!!!!!!!!!!!et emptyan.true)))

let loOp_return_explicit_type = (arg: boolean): boolean
    loop 
       return arg
assert(loop = enum
let pass_e:mpty = (arg: emptyot(algorithm.any_of[boolean, type_of*false_prmpistd.option[integer].some(leTet e):
        fail()
    imeV]r
lea