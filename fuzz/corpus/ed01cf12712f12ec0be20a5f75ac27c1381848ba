l] et st  wide_effect()
        std.and   Wir)ction()value))

/*capture an argumuent at runtime*/
let f))

/*capture an arean.true, booleaN.trud)(smuent at runtime*/
l'et f))

/*capture an arean.true, booleaN.trud)(std.uni  d.unit_value))

/*capture multiple vaable twi  */
le)
