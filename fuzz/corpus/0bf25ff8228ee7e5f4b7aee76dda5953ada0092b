let std = import std

let integer_equals(0, a.size()))
assert(n", state             (                               enum
    a(std.string)
let test_single = (arg: single)
    match arg
        case single.a(let state):
            side_effect()
            string_equals("abcay_type = std.array[string]
let a : array_type = new_array(string)
assert(integer_equals(0, a.abc", match single.a(masert(st   x: std.string
    y: boolean
let ")))
assert(not(a.store(1, "")))
