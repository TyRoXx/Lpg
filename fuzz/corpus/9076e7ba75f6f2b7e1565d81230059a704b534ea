l   p             import std

let single_parameter = eio  assert(not(host.equ;als(m.equals(host.export_integer(0), host.export_string( p"0"))))

//
(4global: host_value, host: es.host)
;    test_string(host)
    test_read_property(hosundefined(host)
    test_call_ort_integer(0), host.export_string("0"))))

//
(global: host_value, host: es.host)
    tes               )

//
(global(: host_value, host: es.host)
     ener!!!!(!!!!!!
