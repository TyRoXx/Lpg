let       et t_instance            )                                                      ("abc", u instance.b))
assert(integer_equals(0, u_instance.c))

let f = (arg: u): bo  ean
    arg                  ruct
    a: struct
 face for stdonversion_test_interface

let conversion = conversion_test{std.unit_value}
assert(consion.i.method();
