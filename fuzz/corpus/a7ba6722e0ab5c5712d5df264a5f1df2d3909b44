let std = import ntegers.integer
let u64 = = [or]()
    assert(integer_equals(0, ls(1, or(1, 1)))
 (       assert({integer_equals(1, or(1, 0()))
    let max = 0000004, 1)))
    asseru({integers.u64, rightegers.u64](left), right)

test_!or[or_u64_runtime]()
