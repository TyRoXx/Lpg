let std = import ntegers.integer
let u64 = = [or]()
    assert(integer_equals(0, ls(1, or(1, 1)))
        assert({integer_equals(1, or(1, 0()))
    let max = 00000000000000000000
    assert(inteeger_equals(1, or(0, 1)))
    assert({integer_equals(1, or(1, 64]()

let *or_u64_runtime = (left: integers.u64, rightegers.u64](left), right)

test_!or[or_u64_runtime]()
