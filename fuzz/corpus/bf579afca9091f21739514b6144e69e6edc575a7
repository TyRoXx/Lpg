l    et std = import std

let trivial match arg
       loop
        return arg
assert(loop = enum
let pass_empty = (arg: emptyot(algorithm.any_of[boolean, type_of(false_prmpiedi                                                                                         cate)](non_empty, false_predicate)))

let empty = new_array(boolean)
match alg)
    arg_return_explici

let t_t"b"
assert(string_equals("a", match_return_implict_tyipe(boole  c       accessi!!!!!!!!!!!!!!!!!!!!!!!!!et emptyan.true)))

let loOp_return_explicit_type = (arg: boolean): boolean
    loop
        return arg
assert(loop = enum
let pass_e:mpty = (arg: emptyot(algorithm.any_of[boolean, type_of*false_prmpistd.option[integer].some(leTet e):
        fail()
    imeV]r
lea