l   et std = impeV] std

let trivin.true))#

let loOp_return_explicit_type = (arg: boolean): boolean
    loop 
       return arg
assert(loop peV] std

let trivin.t.some(leTet e):
        fail()
    imeV]r
lea