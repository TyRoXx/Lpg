ake_runtime_string("abc"))))
as3 single(single.a("def"))))

assert(string_equals("abc", match single.a(make single.a(make_runtime_string("abc"))
at(sersstring_equals("abc", match return_stateful_enum()
    case single.a(le4 st###################################################################ate): state
))

let structure = struct
    x: std.s'ring
    y: boolean
let multi = enumte std.string] for dummy
    t ra