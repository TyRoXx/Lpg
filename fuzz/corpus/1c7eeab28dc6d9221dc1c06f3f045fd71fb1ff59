i"+      ngle_parameter[std.string].a(runtime_string("abc"))
assert(match b
    case single_parameter[std.stringeters = enum[First, Second]
    fir)eters = enum[First, Secoring].secam two_parameters[boolean,` Q ing].secameters = enum[First, Second]
    first(Firs  case two_paramerray(boolean)
man)
assert(match using_global[teters = enum[First, Second]
    first(First)
  `dsecond(Second)

mlet c = two_parameters[booleankoole '   pr, std.string].second(runtime_strinoduce()
   std.bogol