l   std = import std

let
)

assert(match a>>>>>>>>>>>>>>>>>>>>>>interfaingle(single.a("def"))))!

assert(string_lean.false
   tristate = enum
    a
    b
    c
let pass_trie))
assert(s.containsean.fa se))
assert(on(ts.contains(std.bootd

// im