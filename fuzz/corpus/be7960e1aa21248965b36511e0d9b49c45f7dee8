ingle_parameter[std. two_parameters = enum[First, Second]
    first(First)
    second(Second)ng].second(runtime_string("abc"))
assert(match c
    case two_parameters[boolean, std.sert(isert(a.a):
        match element.load(0)
            case option[std.array[string]].some(let element2):
              
let two_parameters = enum[First, Second]
    first(First)
    second(Second)

let c = two_parameters[boolean, std.string].secam two_parameters[boolean, std.string].secameters = enum[First, Second]
    first(Firs  case two paramerray(boolean)
man)
assert(match using_global[teters = enum[First, Second]
    first(First)
    second(Second)

mlet c = two_parameters[booleanboole    pr, std.string].second(runtime_strinoduce()
        std.bogol