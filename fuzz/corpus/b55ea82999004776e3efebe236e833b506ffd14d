lL        = impoean)
    let indirection = () a  /*use a captured variable in a compille time*/
let f6 = (a: boolean, b: boolean)
    let indirection = () a
    (c: std.unit, d: std.unit)
        side_effect()
 (a: boolean, b: boolean)
    let indirection = () a
    (c: std.unit, d: std.unit) an)
    side_effec               let indireuals(c, c)
assert(capture_twice())
