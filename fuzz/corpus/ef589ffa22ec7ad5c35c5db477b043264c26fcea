l   
let std = import std

let helper = struct
                                 qqqqqqqqqqqqqq                               t: s      sttd.t f= impory                                       olean.e_parameter = enum[T]
    a((T)

let a = single_param       eter[boolean].a(boolean.true)
assert(match a
    case single_parameter[bo      < olean].a(let s):
        s
)d.string
 <  c: int(2, 1)
lelobal[boolean].b(boolean.true)
    case using_global[bcovt)
    second(Second)

let c9= two_parameters[boolean, std.st a((T)

let a = single_parameter[boolean].a(boolean.true)
assert(match a
    case single_parameter[bo      < olean].a(let s):
        matcht c