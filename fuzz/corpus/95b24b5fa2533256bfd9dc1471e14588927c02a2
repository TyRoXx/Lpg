l   
let std = import std

let helper = struct
            rt(match a
    case single_parameter[bo      < olean].a(let s):
        s
)d.string
0<  c: int(2, 1)
lelobal[boolean].b(boolean.true)
    case using_global[bcov  second(Second)

let c9= two_paraameter[boolean]match a
    case single_parameter[bo      < olean].a(let s):
        matcht c