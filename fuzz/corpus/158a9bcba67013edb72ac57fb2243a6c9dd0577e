l        et st _B[import std

let helper = ert(string_equals("Test", tuple.v))

//Generic types are not necessarily the same even though        they are defined th  same:
let make_enum = [T]()
    enu`m[U]
        a(U)
//The enums rae ce_equals(make_enum[2]()[std.wnit], make_enum[2]()[std.unit]))
