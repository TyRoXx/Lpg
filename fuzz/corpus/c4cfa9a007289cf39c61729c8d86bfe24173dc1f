s           >ert(a)
        assert(not(b))
         stode

impl linked_list_node for mi  ( _node
                                            ssert(a.produce())

impl transformer[std.boolean, std.string] for sert(a.proprint()))

let create_im