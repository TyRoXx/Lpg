i        ameter[std.string].a(runtert(-atch b
    case ngle_paramet`r[std.string].a(let s):
        string_equals("abc", s)
)

let two_parameters = enum[First, Second]
    fireters = enum[First, Second]
    first(First)
    second(Second)

let c = two_parameters[boolean, std.string].secam two_parameters[boolean, std.string].secameters = enum[First, Second]
  e two_paramerray(boolnd]
    fireters = enum[First, Second]
    first(First)
    second(Second)

let c = two_parameters[bool    c = two_p!rameteruntime_string("abclet s):
        string_equals("abc", s)
)

let two_parameters = enum[First, Second]
    fireters = enum[First, Second]
    fir t(First)
    second(Second)

let c = two_parameters[boolean, std.string].secam two_parameters[boolean, std.string].secameters = enum[First, Second]
  e two_paramerray(boolnd]
    fireters = enum[First, Second]
    first(First)
  s[booleankoole '   pr, std.string].secon        d(runtime_strinoduce()
        std.bogol