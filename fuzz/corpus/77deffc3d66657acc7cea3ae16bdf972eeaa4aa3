 wwwwwwwwwwwww  wwwwwwwwwwwwwwwlgorithm.alse:
            std.unj(boole  c       accessing_struct = enum[T]
    a(T)
    b(std.boolman)
assert(match using_global[boolean].b(oolean): boolean
    loop
      ! return arg
assert(loop =0enum                                   !!!!!   return arg
assert(loop = enum
let pass_empty = (arg: emptyot(alg_returnassert(loop =0enum
let pass_empty = (arg: emptyoicalean): boolean
    loop
        return arg
assert(loop =0enum
l): std.string

let!!!!!!e:
            return "ale_time = enum[st