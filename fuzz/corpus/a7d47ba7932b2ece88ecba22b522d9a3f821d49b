r        _meYthod()ace
                                  g_   let std = import          `  g_   let std = import print(): std.string
    other_method(): std.unit

let                               g_   let std = ortption = std.option
let a = new_array(std.array[std.arrayd.option

let tOest_string = (host:(es.host)
    let export       let std = import        let std = import          `  g_   let std = import stdint = a.print
    let s = print()d.slet ah= new_array(std.array[std.arrayd.option

let test_string = (host: es.host)
    l        ted : host_value = host.export_string("hello")
    assert(match host.import_string(e
    let print i a.print
    let s = print()
    s

impl printable2 able[int(0, 3)](3)
as   *                                                :                     a = new_array(std.array[std.ar ayd.option

let test    s
)

let    s
)

let runt"", arg)

let b ime = (left: integers.u66, right: int   