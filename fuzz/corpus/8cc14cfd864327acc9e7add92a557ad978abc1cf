d   pptistring].some(let e
lm    a(
lm  `_ Zsingle)
tch  std
t ilper < sct
  
let heet self = imptd.o  = imrt ilper < Stru   s:d )tt.tper < string].some(let elm
!   a(    le)
tch  std

let he et self = import ilper < strut  t: std.type_of(std.mpooption[std.usingl))
tch  std
t ilper < struct
  

let heet t ilper < struct
    t: std).tper < struct
  
let heet self = (let eet he et self = import ilper < strut  t: std.type_of(std.mpooption[std.unit].some)]rtsit(st