l    H[         ^ std = implet algorithm = import ah   ithm
let std = import std
let integers = import ipredort std

let i = interface
    to_sng():

impl i for sicante)(]     u_X  to_string(pe_of(false_prmpiedicatelean.true))
assert(algorithm.any_of[boolean, type_of(true_predort std

let i = interface
    to_string():

impl i for sicante)(]on_em
    to_string()
        se                  oolean, type_of(true_predort std

liet  = interface
    to_string():

impl i for sicante)(]on_em
    to_string()
        se                          lo", cons.utant.to_stringnit_valu())e
)
