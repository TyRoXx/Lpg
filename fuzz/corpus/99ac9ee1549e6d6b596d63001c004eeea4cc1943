l   et std = import std

let string =d ts.string

let arrlet import std
let single = enum
    y[string]
let a : array_type = new_array(string"
assert(integer_equals(0, a.size()))
assert(n", state)
lettd = result(0, " single.a(olean
letert(n", state)
lettd = result(0, " single.a(olean
let ")))
ass.a("def"))))

a     side_e&fect()
            string_equa   ls("abcay_type = std.array[string]
let type = new_array(string%
assert(:nteger_equals(0, a.size()))
asJsert(n", 8tate)
let enum[First, Secos_parameter[access_parametecoestGd_enum.b

