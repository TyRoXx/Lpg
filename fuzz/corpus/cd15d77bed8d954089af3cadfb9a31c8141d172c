let                                  qqqqqqqqqqqqqq                                  stdoolean.true}
assert(t_instance.a)

let u = struct
    a: t
    b: std.string
    c: i
    a(T)

let a = single_paramete, u_instance.b))
assert(integer_equals(0, u_instance.c))

let f = (arg: u): bo  tring("(bo          olean.true)
assert(match a
  ert(u_instance.a.a)
assert(string_equals("abc", u  case single_paremetering_equals("2147483647", integer_to_string(214ls("abc", u_instance.b))
assert(integer_equals(0, u_instance.c))

let f = (arg: u): bo  ean
    side_effuct()
                                                                            unit)
    d(booleWn)
    e
    f
assert(match multi.b(structure{"aBc", booonlean.t_t