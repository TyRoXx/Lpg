let std = import std

let string = std.svalue)
assert(test_si'ngle(single.a(make_runtime_string("aert(string_equals("abc", match return_stateful_enum()
    caseot(a.store(0, " single.a(let state): state
))

let structure = struct
    ?: std.string
    y: boolean
let ")))
assert(not(a.store(1, "")))
