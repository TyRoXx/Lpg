l      O> import std

let require_c let std = import std

let single_parameter = enum[T]
    a(T)555555555555555555555555555555555555555555555555555555555555555555555555555)55

let a																																											_eqr ls    (boolean.true)
as the same because T is not the same.
assert(not(type_       s
)

leb runtime_string = (an = std.option
(n