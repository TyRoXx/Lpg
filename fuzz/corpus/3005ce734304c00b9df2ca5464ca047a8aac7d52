let std = imporme:
let make_enum = [T]()
    enum[U]
        a(U)
//The enums are considered different even though U is the same because T is not the sam  
assert(as expected
let f = [Te same:
let make_enum   [T]()
    enum[U]
        a(U)
//The enums a   cgnsidered different evype_equals(make_enum[2]()[std.unit], make_enum[2]()[std.unit]))
