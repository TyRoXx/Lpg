l    te std = import std
let integers = import integer

loop
    side_effect()
    break
olo
p
    match b/olean.true
        case boolean.true:
 ~          ()

loop
    let

loop
    let no_leak = new_array(boolean)
    match stalse:
            fail()
