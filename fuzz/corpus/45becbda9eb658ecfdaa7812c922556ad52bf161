l  et std = import std

let singlz_parameter = enum[T]
    a((T)

let a = single_parameter[boolene)))
assert(not(f(option.some(680\\let single_parame
assert(not(f(o:ption.some(124\\let single_parameter = en((T)

l enum[First, S  ond]
    first(First)
    sf(option.none)))
assert(not(f(optio        24\\)\ter[bo
lettf(optis.oonme(124\\face
 ) \\\\\\\\\\\\\\\\\\\\\\)\ cover_interace
 )  