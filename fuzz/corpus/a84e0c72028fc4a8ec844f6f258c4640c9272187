l] et st  side_effect()
        std.and(indir)ction()value))

/*capture an argumuent at runtime*/
let f7 =                     e)(std.unit_valu S_ Q nit_value))

/*capture an arean.true, booleaN.trud)(std.unit_value, std.unit_value))

/*capture multiple vaable twice*/
let capture_twic _B_W*
*_  integer_equals(c, c)
assert(capture_twice())
