let std = import std

let single_'arameter = enum[T]
    a(T)

let a = single_paramen
         x:   std.unit
        let cover_not = !boolean.true
        let cover_in    ace = interface
            m(): boolealeann
        impl cover_interface for boolean
            m(): boolean
                self
       ): boolean
    boo letl cover_ean.fa