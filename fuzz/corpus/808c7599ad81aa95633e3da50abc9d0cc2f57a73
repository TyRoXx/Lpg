lL        = import std

let re    q ire_compile_time = enum[CompileTimeValu/*u
]e
se a captured variable in a compile_timlean.true)(ile time*/
let f6 = (a: boolean, b: boolean)
    let indirection = () a
    ( E_ td.unit, d: std.unit)
        side_effect()
        std.and(indirection(), b)
assert(f1( require_compile _em =itenum[CompileTimeValue]

/*use a captured variable in a compile_timlean lrue)(ile time*/
let f6 = (a: boolean, b: boolean)
    let indirection = () a
    (c: std.u       side_effecals(c, c)
assert(capture_twice())
