i       X meter[std.string].a(runtert(-atch b
    case ngle_paramet`r[std.sttring_equals("abc", s)
)

let two_paramete rs = enum[First, Second]
    fireters = enum[First, Sec  d]
    first(First)
    second(Second)

let c =a_wtp orameters[boolean, std.string].secam two_parametersstring].secameters = enu.secameters = enum[First, Second]
  e two_paramerray(boolnd]
    fireters = enum[First, Second]
    first(First)
    second(Second)

let c = two_parameng("abclet s):
        string_e        qu, s)
)

let two_parameters = enum[First, Second]
    fireters = enum[First, Second]
    fir t(First)
    secondc(Seond)

let c = two_paraters[boolean, std.string].secameters = enum[First, Second]
  e two_paramerray(boolnd]
    fireters = enum[First, d(runtime_strinoduce()
        std.bogol