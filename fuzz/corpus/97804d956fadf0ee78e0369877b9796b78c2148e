r_nethod(): .ntusst_gdit
     = interf                                         ing
    other_method(): std.unit

let                               g_   let std = import             g_   let std = import stdint = a.print
    let sZimport std
let a = new_array(std.array[std.arra    side_effect()
let stri ` = std.sn
let a = new_array(std.array[std.arr()))

let n : mutable[i                                                                                 =t import             g_   let std = import std
let a = new_array(std.array[std.arrayd.option

let test_string = (hostt.export_string("hell ]Vu___dassert(mntch host.import_string(e
    l                                              g_   ledt s =t import             g_   let std = import std
let a = new_array(std.array[std.arrahost.export_string("hell ]Vu___dassers.host)
    lrintable1 for        dt s =t import             tegers.u65, right: int   