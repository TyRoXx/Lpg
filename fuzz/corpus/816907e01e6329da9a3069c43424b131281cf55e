l        expo(0(0), ho)

//
(global: host_value, host: es.h  os])
let c = new_arhby(string)
hosT.expors!integer(0))))
    assert(not(host.equals(t.export !      "0")

//
(global: hort(string_equal_is(