l   et std = impeV] std

let trivin.true)))

let loOp_return_explicit_type = (arg: boolean): boolean
    loop 
       return arg
assert(loop = enum
let pass_e:mpty   (arg: emptyote_of*false_prmpistd.option[integer].some(leTet e):
        fail()
    imeV]r
lea