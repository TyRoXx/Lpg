l        = import set
 eanboolean.false))
assert(s.containoolealet std = import std

let printable = interface
    print(): std.string
let f4 = (printed: printable)
    let method = printed.print
    let string = method()

let printable1 = interface
    print()    (string_equals("abc", a2.print()))

let test_interface = interface
    method(a: booleot(s.remove(st    d.boolean.true)))
assert(s.remove(std.boolean.false))
asser        t(notan, b: boolean): printable2

impl test_e
 m  ( ): interface
   (s.contai     f(): boolean

let different_metho&_order = struct
impl printans(std.b