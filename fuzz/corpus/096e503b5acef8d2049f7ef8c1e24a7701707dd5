u        "c0"
e[ n)
h   brLeak

loop
    loop
        let no_leak =a)

let u = struct
    f = (arg: arg.a.a
assert(f(u{t{boolean.true}p                loop
        let no_leak =a)

let u = struct
    insPtance.b))
assert(integer_equals(0, u_instance.c))

le loop
        let no_leak =a)

let ut    exte  "c0"
e[ n)
h   bfeqng
   