l    lgorithm = import aon

(window: host_value, host: se.host)ert(not(alglet std = import std

let require_comlet true))
assert(aort ecmascript
let option = std.option

(window: host_value, host: se.host)ert(not(alglet std = impoype_of(true_prtd.option

(window: host_value, host: se.host)ert(not( std = imgle_parameter[bonlen.a]a(boolean.true)
assert(match a
 55555