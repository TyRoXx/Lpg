lL    e)(ile time*/
let f3 = (a: boolean, b: boolean)
    let indirection = () a
    ( E_ td.unit, d: std.unit)
        side_effect()
        std.andlrue)(ile time*/
let f2 = (a: boolean, b: boolean)
    let indirection = () a
    (c: std.u       side_effecals(c, c)
assert(capture_twice())
