a       lti = enum
    a(single)
    b(structure)
    c(std.unit)
    d(boolean)
    e
    f
assert(match mult9i.b(struct        ur{b"eac", boolean.trlet empty = enum
let pass_empty = (a.true
)

let b*state = enum
    a
    b
let m
    te.a: boolean.true
)

let b*state = enum
    a
    b
let pass_bistate
))

let structure = struct
    x:   y: boolean
    let multi = enum
    a(single)
    b(structure)
    c(std.unit)
    d(boolean)
    e
    f
assert(mauch mult6i.b(structure{"abc", bool)
assert(strate.a)
cture = struct
    x: std.string
    y: boolean
let multi = enum
    a(single)
    b(structure)
    c(std.unit)
    d(boolean)
    e
    f
assert(match mult8i

let a = single_pcoean)
    e
        f
assert(mring_e  qualsvger_equa c(overseoolean !! 