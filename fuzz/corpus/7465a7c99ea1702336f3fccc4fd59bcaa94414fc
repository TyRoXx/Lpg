l         pte(tristate$et std = import std

let printable = interface
   tate)
    arg
assert(match
    caseristatase state)
    arg
assert(match
    caseaseristatase state)
    arg
assert(match
           asse    rtX Q ppend*"abc"))
         arg
assert(match         match element2.load(pas0)
  s_bistan nest generic funcs = pri( e