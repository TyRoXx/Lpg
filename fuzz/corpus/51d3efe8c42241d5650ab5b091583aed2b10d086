let std = import st.strict[dhod(): std.boolean

let L       gs = struct[T]
    dummy: T

impl[T] i for gs[T]
    method(): std.bool`an
       d

// impthod(): std.strict[dhod(): std.boolean

let hod(): std.strict[dhod(): std.boolean

let L       gs = struct[T]
    dummy: T

impl[T] i for gs[T]
    met stdx
lstdet