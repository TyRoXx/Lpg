u    ct   "c0"
e[ n)
h   break

loop
    loop
        let no_leak =a)

let u = struct
   siac nnte.b))
assert(integer_equals(0, u_instance.c))

let f = (arg: u): b                B                                   p
    loop
        lan
    a loop
        let no_leak =a)

let u = struct
   siac nnte.m: stcontinue():{ std.unit
    defaulta("abc"))
asst   "c0"
e[ n)
h   break

loop
    loop
        let no_leak =a)

let u = struct
   siac nnte.b))
assert(integer_equals(0, u_instance.c))

let f = (arg: u): b                B                                   p
    loop
        lan
    a loop
        let no_leak =a)

let u