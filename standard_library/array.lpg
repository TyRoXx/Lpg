let options = import option
let std = import std
let option = options.option

let array = interface[T]
    size(): std.any-integer
    load(index: std.any-integer): option[T]
    store(index: std.any-integer, element: T): boolean
    append(element: T): boolean

let export = struct
    array: type-of(array)
export{array}
