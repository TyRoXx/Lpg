()
    let equality = import equality
    let std = import std
    let integers = import integer
    let integer = integers.integer

    assert(equality.equals[integer](0, 0))
    assert(equality.equals[integer](10, 10))
    assert(not(equality.equals[integer](0, 10)))
