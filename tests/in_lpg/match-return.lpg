let always-return-from-match = (arg: boolean)
    match arg
        case boolean.false:
            return 1
        case boolean.true:
            return 2
    assert(boolean.false)
assert(integer_equals(1, always-return-from-match(boolean.false)))
assert(integer_equals(2, always-return-from-match(boolean.true)))
