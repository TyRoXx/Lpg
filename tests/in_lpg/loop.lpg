loop
    side-effect()
    break

loop
    match boolean.true
        case boolean.true:
            break
        case boolean.false: unit_value

loop
    loop
        break
        assert(boolean.false)
    break
    assert(boolean.false)
