let std = import std
let option = std.option

let runtime = (arg: std.boolean)
    side_effect()
    option[std.boolean].some(arg)
runtime(std.boolean.true)

let compile-time = (arg: std.boolean)
    option[std.boolean].some(arg)
compile-time(std.boolean.false)
