let std = import std
let option = std.option

let runtime = (arg: int(0, 1000))
    side-effect()
    option.some(arg)
runtime(123)

let compile-time = (arg: int(0, 1000))
    option.some(arg)
compile-time(123)
