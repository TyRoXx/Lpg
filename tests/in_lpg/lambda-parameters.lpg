let std = import std
let option = std.option-int
let f = (a: boolean, b: std.string-ref, c: option, d: int(0, 2))
    side-effect()
    assert(a)
    assert(string-equals("abc", b))
    assert(integer-equals(1, d))
f(boolean.true, "abc", option.some(2), 1)
